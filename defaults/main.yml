---
# Zabbix defaults

zabbix_version: "7.4"
zabbix_release: "zabbix-release_{{ zabbix_version }}+debian$(lsb_release -rs)_all.deb"
zabbix_repo_url: "https://repo.zabbix.com/zabbix/{{ zabbix_version }}/debian/pool/main/z/zabbix-release/{{ zabbix_release }}"
